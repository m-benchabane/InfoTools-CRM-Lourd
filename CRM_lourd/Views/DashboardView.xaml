<UserControl x:Class="CRM_lourd.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200"
             FontFamily="Segoe UI"
             TextOptions.TextFormattingMode="Display"
             UseLayoutRounding="True"
             SnapsToDevicePixels="True">

    <UserControl.Resources>
        <SolidColorBrush x:Key="PrimaryColor" Color="#2563EB"/>
        <SolidColorBrush x:Key="BackgroundColor" Color="#F3F4F6"/>
        <SolidColorBrush x:Key="SurfaceColor" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#111827"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#6B7280"/>
        <SolidColorBrush x:Key="BorderColor" Color="#E5E7EB"/>

        <Geometry x:Key="IconDashboard">M13,3V9H21V3M13,21H21V11H13M3,21H11V15H3M3,13H11V3H3V13Z</Geometry>
        <Geometry x:Key="IconClients">M12,4A4,4 0 0,1 16,8A4,4 0 0,1 12,12A4,4 0 0,1 8,8A4,4 0 0,1 12,4M12,14C16.42,14 20,15.79 20,18V20H4V18C4,15.79 7.58,14 12,14Z</Geometry>
        <Geometry x:Key="IconCalendar">M19,19H5V8H19M19,3H18V1H16V3H8V1H6V3H5C3.89,3 3,3.9 3,5V19A2,2 0 0,0 5,21H19A2,2 0 0,0 21,19V5A2,2 0 0,0 19,3M16.53,11.06L15.47,10L10.59,14.88L8.47,12.76L7.41,13.82L10.59,17Z</Geometry>
        <Geometry x:Key="IconInvoice">M14,2H6A2,2 0 0,0 4,4V20A2,2 0 0,0 6,22H18A2,2 0 0,0 20,20V8L14,2M11,17H9V11H11V17M15,17H13V14H15V17Z</Geometry>
        <Geometry x:Key="IconProduct">M21,16.5C21,16.88 20.79,17.21 20.47,17.38L12.57,21.82C12.41,21.94 12.21,22 12,22C11.79,22 11.59,21.94 11.43,21.82L3.53,17.38C3.21,17.21 3,16.88 3,16.5V7.5C3,7.12 3.21,6.79 3.53,6.62L11.43,2.18C11.59,2.06 11.79,2 12,2C12.21,2 12.41,2.06 12.57,2.18L20.47,6.62C20.79,6.79 21,7.12 21,7.5V16.5M12,4.15L6.04,7.5L12,10.85L17.96,7.5L12,4.15M5,15.91L11,19.29V12.58L5,9.21V15.91M19,15.91V9.21L13,12.58V19.29L19,15.91Z</Geometry>

        <Style x:Key="KpiCard" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource SurfaceColor}"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Margin" Value="0,0,20,0"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="15" ShadowDepth="4" Opacity="0.05" Color="#000000"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DashboardDataGrid" TargetType="DataGrid">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="RowHeight" Value="50"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="CanUserAddRows" Value="False"/>
        </Style>
    </UserControl.Resources>

    <Grid Background="{StaticResource BackgroundColor}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="1.5*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource SurfaceColor}" Padding="30,20" BorderBrush="{StaticResource BorderColor}" BorderThickness="0,0,0,1">
            <Grid>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Path Data="{StaticResource IconDashboard}" Fill="{StaticResource PrimaryColor}" Width="24" Height="24" Stretch="Uniform" Margin="0,0,15,0"/>
                    <TextBlock Text="Tableau de Bord" FontSize="24" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" VerticalAlignment="Center"/>
                </StackPanel>

                <Button Content="📥 Exporter PDF" 
                        HorizontalAlignment="Right" 
                        Padding="15,8" 
                        Background="{StaticResource PrimaryColor}" 
                        Foreground="White" 
                        FontWeight="Bold"
                        Command="{Binding ExportPdfCommand}">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="6"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </Grid>
        </Border>

        <Grid Grid.Row="1" Margin="30,30,30,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource KpiCard}">
                <StackPanel Orientation="Horizontal">
                    <Border Width="50" Height="50" CornerRadius="25" Background="#EFF6FF" Margin="0,0,15,0">
                        <Path Data="{StaticResource IconClients}" Fill="{StaticResource PrimaryColor}" Width="24" Height="24" Stretch="Uniform"/>
                    </Border>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="{Binding ClientsCount}" FontSize="26" FontWeight="Bold"/>
                        <TextBlock Text="Clients Totaux" FontSize="13" Foreground="{StaticResource TextSecondary}"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <Border Grid.Column="1" Style="{StaticResource KpiCard}">
                <StackPanel Orientation="Horizontal">
                    <Border Width="50" Height="50" CornerRadius="25" Background="#ECFDF5" Margin="0,0,15,0">
                        <Path Data="{StaticResource IconCalendar}" Fill="#10B981" Width="24" Height="24" Stretch="Uniform"/>
                    </Border>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="{Binding UpcomingAppointmentsCount}" FontSize="26" FontWeight="Bold"/>
                        <TextBlock Text="RDV à venir" FontSize="13" Foreground="{StaticResource TextSecondary}"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Style="{StaticResource KpiCard}">
                <StackPanel Orientation="Horizontal">
                    <Border Width="50" Height="50" CornerRadius="25" Background="#FFFBEB" Margin="0,0,15,0">
                        <Path Data="{StaticResource IconInvoice}" Fill="#F59E0B" Width="24" Height="24" Stretch="Uniform"/>
                    </Border>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="{Binding MonthlyInvoicesCount}" FontSize="26" FontWeight="Bold"/>
                        <TextBlock Text="Factures (Mois)" FontSize="13" Foreground="{StaticResource TextSecondary}"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <Border Grid.Column="3" Style="{StaticResource KpiCard}" Margin="0">
                <StackPanel Orientation="Horizontal">
                    <Border Width="50" Height="50" CornerRadius="25" Background="#F5F3FF" Margin="0,0,15,0">
                        <Path Data="{StaticResource IconProduct}" Fill="#8B5CF6" Width="24" Height="24" Stretch="Uniform"/>
                    </Border>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="{Binding ProductsCount}" FontSize="26" FontWeight="Bold"/>
                        <TextBlock Text="Produits Actifs" FontSize="13" Foreground="{StaticResource TextSecondary}"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>

        <Border Grid.Row="2" Background="{StaticResource SurfaceColor}" CornerRadius="12" Margin="30,10,30,20">
            <Grid Margin="25">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Analyse des Performances" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>
                <lvc:CartesianChart Grid.Row="1" Series="{Binding StatsSeries}">
                    <lvc:CartesianChart.AxisX>
                        <lvc:Axis Labels="{Binding StatsLabels}"/>
                    </lvc:CartesianChart.AxisX>
                </lvc:CartesianChart>
            </Grid>
        </Border>

        <Border Grid.Row="3" Background="{StaticResource SurfaceColor}" CornerRadius="12" Margin="30,0,30,30">
            <Grid Margin="25,20,25,25">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Derniers Clients Inscrits" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>
                <DataGrid Grid.Row="1" ItemsSource="{Binding RecentClients}" Style="{StaticResource DashboardDataGrid}">
                    <DataGrid.Columns>
                        <DataGridTemplateColumn Header="CLIENT" Width="1.5*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Border Width="35" Height="35" CornerRadius="17.5" Background="#DBEAFE" Margin="0,0,12,0">
                                            <TextBlock Text="{Binding Initial}" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{StaticResource PrimaryColor}" FontWeight="Bold"/>
                                        </Border>
                                        <TextBlock Text="{Binding Name}" VerticalAlignment="Center" FontWeight="Medium"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="EMAIL" Binding="{Binding Email}" Width="*"/>
                        <DataGridTextColumn Header="TÉLÉPHONE" Binding="{Binding Phone}" Width="0.8*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>