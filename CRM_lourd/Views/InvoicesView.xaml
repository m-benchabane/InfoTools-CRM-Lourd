<UserControl x:Class="CRM_lourd.Views.InvoicesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="750" d:DesignWidth="1200"
             FontFamily="Segoe UI">

    <UserControl.Resources>
        <SolidColorBrush x:Key="PrimaryColor" Color="#2563EB"/>
        <SolidColorBrush x:Key="PrimaryHover" Color="#1D4ED8"/>
        <SolidColorBrush x:Key="DangerColor" Color="#EF4444"/>
        <SolidColorBrush x:Key="DangerHover" Color="#DC2626"/>
        <SolidColorBrush x:Key="BackgroundColor" Color="#F3F4F6"/>
        <SolidColorBrush x:Key="SurfaceColor" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#111827"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#6B7280"/>
        <SolidColorBrush x:Key="BorderColor" Color="#E5E7EB"/>

        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border CornerRadius="6" Background="White" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryColor}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="OutlineButton" TargetType="Button" BasedOn="{StaticResource ActionButton}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource PrimaryColor}"/>
            <Setter Property="BorderBrush" Value="{StaticResource PrimaryColor}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DeleteButton" TargetType="Button" BasedOn="{StaticResource ActionButton}">
            <Setter Property="Background" Value="{StaticResource DangerColor}"/>
        </Style>

        <Style x:Key="ModernDataGrid" TargetType="DataGrid">
            <Setter Property="Background" Value="White"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#F3F4F6"/>
        </Style>
    </UserControl.Resources>

    <Grid Background="{StaticResource BackgroundColor}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Background="{StaticResource SurfaceColor}" Padding="30,20" BorderBrush="{StaticResource BorderColor}" BorderThickness="0,0,0,1">
            <TextBlock Text="Facturation &amp; Commandes" FontSize="24" FontWeight="Bold" Foreground="{StaticResource TextPrimary}"/>
        </Border>

        <Grid Grid.Row="1" Margin="30">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="380"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="White" CornerRadius="12" Padding="25">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="15" Opacity="0.05"/>
                </Border.Effect>
                <StackPanel>
                    <TextBlock Text="Détails Facture" FontSize="18" FontWeight="Bold" Margin="0,0,0,20"/>

                    <TextBlock Text="Client" Foreground="{StaticResource TextSecondary}" Margin="0,0,0,5"/>
                    <ComboBox x:Name="cbClients" DisplayMemberPath="Name" Height="40" Margin="0,0,0,15" SelectionChanged="cbClients_SelectionChanged"/>

                    <TextBlock Text="Date" Foreground="{StaticResource TextSecondary}" Margin="0,0,0,5"/>
                    <DatePicker x:Name="dpInvoiceDate" Height="40" Margin="0,0,0,20"/>

                    <Separator Margin="0,0,0,20"/>

                    <TextBlock Text="Produit" Foreground="{StaticResource TextSecondary}" Margin="0,0,0,5"/>
                    <Grid Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="80"/>
                        </Grid.ColumnDefinitions>
                        <ComboBox x:Name="cbProducts" Grid.Column="0" DisplayMemberPath="Name" Height="40" Margin="0,0,10,0"/>
                        <TextBox x:Name="txtQty" Grid.Column="1" Text="1" Height="40" VerticalContentAlignment="Center" HorizontalContentAlignment="Center"/>
                    </Grid>

                    <Button Content="Ajouter à la liste" Click="btnAddLine_Click" Margin="0,0,0,20" Background="#10B981"/>

                    <ListBox x:Name="lbBasket" Height="120" Margin="0,0,0,10" BorderBrush="{StaticResource BorderColor}">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid Width="300">
                                    <TextBlock Text="{Binding ProductName}"/>
                                    <TextBlock Text="{Binding LineTotal, StringFormat={}{0:N2} €}" HorizontalAlignment="Right" FontWeight="Bold"/>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>

                    <TextBlock Text="TOTAL GÉNÉRAL" HorizontalAlignment="Right" FontSize="12" Foreground="{StaticResource TextSecondary}"/>
                    <TextBox x:Name="txtInvoiceTotal" Style="{StaticResource ModernTextBox}" IsReadOnly="True" FontWeight="Bold" FontSize="20" TextAlignment="Right" Margin="0,0,0,20"/>

                    <Button Content="Enregistrer Nouveau" Click="btnAddInvoice_Click" Style="{StaticResource ActionButton}" Margin="0,0,0,10"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Button Grid.Column="0" Content="Mettre à jour" Click="btnUpdateInvoice_Click" Style="{StaticResource OutlineButton}"/>
                        <Button Grid.Column="2" Content="Supprimer" Click="btnDeleteInvoice_Click" Style="{StaticResource DeleteButton}"/>
                    </Grid>
                    <Button Content="Réinitialiser" Click="btnReset_Click" Margin="0,10,0,0" Background="Transparent" Foreground="{StaticResource TextSecondary}" BorderThickness="0"/>
                </StackPanel>
            </Border>

            <Grid Grid.Column="1" Margin="25,0,0,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="200"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Background="White" CornerRadius="12" Margin="0,0,0,20">
                    <DockPanel>
                        <TextBlock DockPanel.Dock="Top" Text="Historique Client" FontWeight="Bold" Margin="20,15"/>
                        <DataGrid x:Name="dgInvoices" Style="{StaticResource ModernDataGrid}" SelectionChanged="dgInvoices_SelectionChanged">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="N°" Binding="{Binding Id}" Width="60"/>
                                <DataGridTextColumn Header="Date" Binding="{Binding InvoicedAt, StringFormat=dd/MM/yyyy}" Width="*"/>
                                <DataGridTextColumn Header="Montant" Binding="{Binding Total, StringFormat={}{0:N2} €}" Width="*"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </DockPanel>
                </Border>

                <Border Grid.Row="2" Background="White" CornerRadius="12">
                    <DockPanel>
                        <TextBlock DockPanel.Dock="Top" Text="Lignes de la facture sélectionnée" FontWeight="Bold" Margin="20,15"/>
                        <DataGrid x:Name="dgInvoiceItems" Style="{StaticResource ModernDataGrid}" IsReadOnly="True">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Produit" Binding="{Binding ProductName}" Width="2*"/>
                                <DataGridTextColumn Header="Qté" Binding="{Binding Qty}" Width="60"/>
                                <DataGridTextColumn Header="P.U." Binding="{Binding UnitPrice, StringFormat={}{0:N2} €}" Width="100"/>
                                <DataGridTextColumn Header="Total" Binding="{Binding LineTotal, StringFormat={}{0:N2} €}" Width="100"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </DockPanel>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</UserControl>