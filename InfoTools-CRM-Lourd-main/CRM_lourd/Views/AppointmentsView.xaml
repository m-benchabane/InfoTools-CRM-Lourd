<UserControl x:Class="CRM_lourd.Views.AppointmentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="1200"
             FontFamily="Segoe UI">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <SolidColorBrush x:Key="PrimaryColor"    Color="#2563EB"/>
        <SolidColorBrush x:Key="PrimaryHover"    Color="#1D4ED8"/>
        <SolidColorBrush x:Key="PrimaryLight"    Color="#EFF6FF"/>
        <SolidColorBrush x:Key="DangerColor"     Color="#EF4444"/>
        <SolidColorBrush x:Key="DangerBg"        Color="#FEE2E2"/>
        <SolidColorBrush x:Key="WarnColor"       Color="#D97706"/>
        <SolidColorBrush x:Key="WarnBg"          Color="#FEF3C7"/>
        <SolidColorBrush x:Key="WarnLight"       Color="#FFFBEB"/>
        <SolidColorBrush x:Key="BackgroundColor" Color="#F1F5F9"/>
        <SolidColorBrush x:Key="SurfaceColor"    Color="#FFFFFF"/>
        <SolidColorBrush x:Key="SurfaceHover"    Color="#F8FAFC"/>
        <SolidColorBrush x:Key="TextPrimary"     Color="#0F172A"/>
        <SolidColorBrush x:Key="TextSecondary"   Color="#64748B"/>
        <SolidColorBrush x:Key="TextMuted"       Color="#94A3B8"/>
        <SolidColorBrush x:Key="BorderColor"     Color="#E2E8F0"/>
        <SolidColorBrush x:Key="BorderFocus"     Color="#2563EB"/>

        <!-- ── TextBox ── -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Padding"         Value="10,9"/>
            <Setter Property="FontSize"        Value="13"/>
            <Setter Property="Foreground"      Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderBrush"     Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background"      Value="{StaticResource SurfaceColor}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" CornerRadius="7"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false"
                                          VerticalAlignment="Center" Margin="2,0"
                                          HorizontalScrollBarVisibility="Hidden"
                                          VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource BorderFocus}"/>
                                <Setter TargetName="border" Property="Background"  Value="{StaticResource PrimaryLight}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#94A3B8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ── Primary Button ── -->
        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Background"      Value="{StaticResource PrimaryColor}"/>
            <Setter Property="Foreground"      Value="White"/>
            <Setter Property="Padding"         Value="16,9"/>
            <Setter Property="FontSize"        Value="13"/>
            <Setter Property="FontWeight"      Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor"          Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="7">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource PrimaryHover}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#1E3A8A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ── Ghost Button ── -->
        <Style x:Key="GhostButton" TargetType="Button">
            <Setter Property="Background"      Value="Transparent"/>
            <Setter Property="Foreground"      Value="{StaticResource PrimaryColor}"/>
            <Setter Property="BorderBrush"     Value="{StaticResource PrimaryColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding"         Value="12,8"/>
            <Setter Property="FontSize"        Value="13"/>
            <Setter Property="FontWeight"      Value="Medium"/>
            <Setter Property="Cursor"          Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="7"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource PrimaryLight}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ── Danger Button ── -->
        <Style x:Key="DangerButton" TargetType="Button">
            <Setter Property="Background"      Value="{StaticResource DangerBg}"/>
            <Setter Property="Foreground"      Value="{StaticResource DangerColor}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding"         Value="12,8"/>
            <Setter Property="FontSize"        Value="13"/>
            <Setter Property="FontWeight"      Value="Medium"/>
            <Setter Property="Cursor"          Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="7" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FECACA"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ── Onglet actif Bleu (Clients) ── -->
        <Style x:Key="TabButtonActive" TargetType="Button">
            <Setter Property="Background"      Value="{StaticResource PrimaryColor}"/>
            <Setter Property="Foreground"      Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding"         Value="20,8"/>
            <Setter Property="FontSize"        Value="13"/>
            <Setter Property="FontWeight"      Value="SemiBold"/>
            <Setter Property="Cursor"          Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="7" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ── Onglet inactif ── -->
        <Style x:Key="TabButtonInactive" TargetType="Button">
            <Setter Property="Background"      Value="Transparent"/>
            <Setter Property="Foreground"      Value="{StaticResource TextMuted}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding"         Value="20,8"/>
            <Setter Property="FontSize"        Value="13"/>
            <Setter Property="FontWeight"      Value="Medium"/>
            <Setter Property="Cursor"          Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="7" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource BorderColor}"/>
                                <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ── Input Label ── -->
        <Style x:Key="InputLabel" TargetType="TextBlock">
            <Setter Property="Foreground"  Value="{StaticResource TextSecondary}"/>
            <Setter Property="FontWeight"  Value="SemiBold"/>
            <Setter Property="FontSize"    Value="12"/>
            <Setter Property="Margin"      Value="0,0,0,5"/>
        </Style>

        <!-- ── Section Title ── -->
        <Style x:Key="SectionTitle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="FontSize"   Value="15"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>

        <!-- ── Style DataGridCell commun ── -->
        <Style x:Key="GridCellStyle" TargetType="DataGridCell">
            <Setter Property="BorderThickness"   Value="0"/>
            <Setter Property="VerticalAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Background="Transparent" Padding="14,0">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

    </UserControl.Resources>

    <!-- ROOT -->
    <Grid Background="{StaticResource BackgroundColor}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- TOP BAR -->
        <Border Background="{StaticResource SurfaceColor}"
                BorderBrush="{StaticResource BorderColor}"
                BorderThickness="0,0,0,1"
                Padding="32,0">
            <Border.Effect>
                <DropShadowEffect Color="#CBD5E1" BlurRadius="8" ShadowDepth="1" Opacity="0.35" Direction="270"/>
            </Border.Effect>
            <Grid Height="64">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Background="{StaticResource PrimaryColor}" CornerRadius="6"
                            Padding="10,5" Margin="0,0,14,0" VerticalAlignment="Center">
                        <TextBlock Text="CRM" Foreground="White" FontSize="11" FontWeight="Bold"/>
                    </Border>
                    <Path Data="M19,4H18V2H16V4H8V2H6V4H5C3.89,4 3,4.9 3,6V20A2,2 0 0,0 5,22H19A2,2 0 0,0 21,20V6A2,2 0 0,0 19,4M19,20H5V10H19V20M19,8H5V6H19V8M9,14H7V12H9V14M13,14H11V12H13V14M17,14H15V12H17V14M9,18H7V16H9V18M13,18H11V16H13V18M17,18H15V16H17V18Z"
                          Fill="{StaticResource PrimaryColor}"
                          Width="18" Height="18" Stretch="Uniform"
                          Margin="0,0,10,0" VerticalAlignment="Center"/>
                    <TextBlock Text="Rendez-vous"
                               FontSize="18" FontWeight="Bold"
                               Foreground="{StaticResource TextPrimary}"
                               VerticalAlignment="Center"/>
                    <Border Width="1" Height="20" Background="{StaticResource BorderColor}"
                            Margin="16,0" VerticalAlignment="Center"/>
                    <TextBlock Text="Planifiez et suivez vos rendez-vous"
                               FontSize="13" Foreground="{StaticResource TextMuted}"
                               VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- MAIN CONTENT -->
        <Grid Grid.Row="1" Margin="28">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="360"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- LEFT PANEL — Formulaire -->
            <Border Grid.Column="0"
                    Background="{StaticResource SurfaceColor}"
                    CornerRadius="12"
                    Margin="0,0,20,0"
                    BorderBrush="{StaticResource BorderColor}"
                    BorderThickness="1"
                    VerticalAlignment="Top">
                <Border.Effect>
                    <DropShadowEffect Color="#CBD5E1" BlurRadius="14" ShadowDepth="2" Opacity="0.2" Direction="270"/>
                </Border.Effect>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="4"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Border Grid.Row="0" CornerRadius="12,12,0,0" Background="{StaticResource PrimaryColor}"/>
                    <StackPanel Grid.Row="1" Margin="24,20,24,24">

                        <!-- Header -->
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                            <Border Width="3" CornerRadius="2" Background="{StaticResource PrimaryColor}" Margin="0,0,10,0"/>
                            <TextBlock Text="Détails du Rendez-vous" Style="{StaticResource SectionTitle}" VerticalAlignment="Center"/>
                        </StackPanel>

                        <!-- Client -->
                        <StackPanel Margin="0,0,0,14">
                            <TextBlock Text="Client / Prospect" Style="{StaticResource InputLabel}"/>
                            <ComboBox x:Name="cbClients"
                                      Height="38" Padding="10,8" FontSize="13"
                                      Background="White"
                                      BorderBrush="{StaticResource BorderColor}"
                                      SelectionChanged="cbClients_SelectionChanged"/>
                        </StackPanel>

                        <!-- ══ COMMERCIAL (visible uniquement pour Manager/Admin) ══ -->
                        <StackPanel x:Name="pnlCommercial" Margin="0,0,0,14">
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                                <Path Data="M12,4A4,4 0 0,1 16,8A4,4 0 0,1 12,12A4,4 0 0,1 8,8A4,4 0 0,1 12,4M12,14C16.42,14 20,15.79 20,18V20H4V18C4,15.79 7.58,14 12,14Z"
                                      Fill="{StaticResource PrimaryColor}"
                                      Width="12" Height="12" Stretch="Uniform"
                                      VerticalAlignment="Center" Margin="0,0,6,0"/>
                                <TextBlock Text="COMMERCIAL ASSIGNÉ" Style="{StaticResource InputLabel}"
                                           VerticalAlignment="Center" Margin="0"/>
                            </StackPanel>
                            <!-- Fond légèrement teinté pour signaler la zone admin -->
                            <Border CornerRadius="8" BorderBrush="#BFDBFE" BorderThickness="1"
                                    Background="#EFF6FF" Padding="2">
                                <ComboBox x:Name="cbCommerciaux"
                                          Height="36" Padding="10,8" FontSize="13"
                                          Background="Transparent"
                                          BorderThickness="0"
                                          SelectionChanged="cbCommerciaux_SelectionChanged"/>
                            </Border>
                            <TextBlock x:Name="txtCommercialInfo"
                                       Text="Vous assignez ce RDV en tant que Manager"
                                       FontSize="11" Foreground="{StaticResource PrimaryColor}"
                                       Margin="0,4,0,0" FontStyle="Italic"/>
                        </StackPanel>

                        <!-- Date -->
                        <StackPanel Margin="0,0,0,14">
                            <TextBlock Text="Date prévue" Style="{StaticResource InputLabel}"/>
                            <DatePicker x:Name="dpAppointmentDate"
                                        Height="38"
                                        VerticalContentAlignment="Center"
                                        BorderBrush="{StaticResource BorderColor}"
                                        BorderThickness="1"/>
                        </StackPanel>

                        <!-- Sujet -->
                        <StackPanel Margin="0,0,0,22">
                            <TextBlock Text="Sujet" Style="{StaticResource InputLabel}"/>
                            <TextBox x:Name="txtAppointmentSubject"
                                     Style="{StaticResource ModernTextBox}"
                                     Height="80"
                                     TextWrapping="Wrap"
                                     VerticalContentAlignment="Top"
                                     Padding="10,8"/>
                        </StackPanel>

                        <Border Height="1" Background="{StaticResource BorderColor}" Margin="0,0,0,16"/>

                        <!-- Actions -->
                        <Button Click="btnAddAppointment_Click"
                                Style="{StaticResource ActionButton}"
                                Height="40"
                                Margin="0,0,0,10">
                            <StackPanel Orientation="Horizontal">
                                <Path Data="M19,13H13V19H11V13H5V11H11V5H13V11H19V13Z"
                                      Fill="White" Width="13" Height="13"
                                      Stretch="Uniform" Margin="0,0,8,0"/>
                                <TextBlock Text="Planifier le rendez-vous"/>
                            </StackPanel>
                        </Button>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="8"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Button Grid.Column="0"
                                    Click="btnUpdateAppointment_Click"
                                    Style="{StaticResource GhostButton}"
                                    Height="36">
                                <StackPanel Orientation="Horizontal">
                                    <Path Data="M20.71,7.04C21.1,6.65 21.1,6 20.71,5.63L18.37,3.29C18,2.9 17.35,2.9 16.96,3.29L15.12,5.12L18.87,8.87M3,17.25V21H6.75L17.81,9.93L14.06,6.18L3,17.25Z"
                                          Fill="{StaticResource PrimaryColor}"
                                          Width="12" Height="12" Stretch="Uniform" Margin="0,0,7,0"/>
                                    <TextBlock Text="Modifier"/>
                                </StackPanel>
                            </Button>
                            <Button Grid.Column="2"
                                    Click="btnDeleteAppointment_Click"
                                    Style="{StaticResource DangerButton}"
                                    Height="36">
                                <StackPanel Orientation="Horizontal">
                                    <Path Data="M19,4H15.5L14.5,3H9.5L8.5,4H5V6H19M6,19A2,2 0 0,0 8,21H16A2,2 0 0,0 18,19V7H6V19Z"
                                          Fill="{StaticResource DangerColor}"
                                          Width="12" Height="12" Stretch="Uniform" Margin="0,0,7,0"/>
                                    <TextBlock Text="Supprimer"/>
                                </StackPanel>
                            </Button>
                        </Grid>

                    </StackPanel>
                </Grid>
            </Border>

            <!-- RIGHT PANEL — Liste avec onglets -->
            <Border Grid.Column="1"
                    Background="{StaticResource SurfaceColor}"
                    CornerRadius="12"
                    BorderBrush="{StaticResource BorderColor}"
                    BorderThickness="1">
                <Border.Effect>
                    <DropShadowEffect Color="#CBD5E1" BlurRadius="14" ShadowDepth="2" Opacity="0.2" Direction="270"/>
                </Border.Effect>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <!-- Header + onglets -->
                        <RowDefinition Height="Auto"/>
                        <!-- Barre de recherche -->
                        <RowDefinition Height="*"/>
                        <!-- DataGrid -->
                    </Grid.RowDefinitions>

                    <!-- Header avec onglets -->
                    <Border Grid.Row="0"
                            Padding="18,14"
                            BorderBrush="{StaticResource BorderColor}"
                            BorderThickness="0,0,0,1">
                        <Grid>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <TextBlock Text="Rendez-vous " Style="{StaticResource SectionTitle}" VerticalAlignment="Center"/>
                                <TextBlock Text="Planifiés" Foreground="{StaticResource PrimaryColor}"
                                           FontSize="15" FontWeight="Bold" VerticalAlignment="Center"/>
                            </StackPanel>

                            <!-- Onglets Clients / Prospects -->
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right"
                                        VerticalAlignment="Center">
                                <Border Background="{StaticResource BackgroundColor}"
                                        CornerRadius="8" Padding="4">
                                    <StackPanel Orientation="Horizontal">
                                        <Button x:Name="btnTabClients"
                                                Content="Clients"
                                                Style="{StaticResource TabButtonActive}"
                                                Height="32"
                                                Click="btnTabClients_Click"/>
                                        <Button x:Name="btnTabProspects"
                                                Content="Prospects"
                                                Style="{StaticResource TabButtonInactive}"
                                                Height="32"
                                                Margin="4,0,0,0"
                                                Click="btnTabProspects_Click"/>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <!-- Barre de recherche -->
                    <Border Grid.Row="1"
                            Padding="18,12"
                            BorderBrush="{StaticResource BorderColor}"
                            BorderThickness="0,0,0,1">
                        <Grid>
                            <TextBox x:Name="txtSearch"
                                     Style="{StaticResource ModernTextBox}"
                                     Padding="36,9,10,9"
                                     Height="38"/>
                            <Path Data="M9.5,3A6.5,6.5 0 0,1 16,9.5C16,11.11 15.41,12.59 14.44,13.73L14.71,14H15.5L20.5,19L19,20.5L14,15.5V14.71L13.73,14.44C12.59,15.41 11.11,16 9.5,16A6.5,6.5 0 0,1 3,9.5A6.5,6.5 0 0,1 9.5,3M9.5,5C7,5 5,7 5,9.5C5,12 7,14 9.5,14C12,14 14,12 14,9.5C14,7 12,5 9.5,5Z"
                                  Fill="{StaticResource TextMuted}"
                                  Width="15" Height="15" Stretch="Uniform"
                                  HorizontalAlignment="Left" Margin="30,0,0,0"
                                  VerticalAlignment="Center" IsHitTestVisible="False"/>
                        </Grid>
                    </Border>

                    <!-- DataGrid -->
                    <DataGrid x:Name="dgAppointments"
                              Grid.Row="2"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              IsReadOnly="True"
                              SelectionMode="Single"
                              SelectionChanged="dgAppointments_SelectionChanged"
                              GridLinesVisibility="Horizontal"
                              HorizontalGridLinesBrush="{StaticResource BorderColor}"
                              RowHeight="50"
                              Background="Transparent"
                              BorderThickness="0"
                              ColumnHeaderHeight="40">

                        <DataGrid.Resources>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background"      Value="Transparent"/>
                                <Setter Property="Foreground"      Value="{StaticResource TextMuted}"/>
                                <Setter Property="Padding"         Value="14,0"/>
                                <Setter Property="FontWeight"      Value="SemiBold"/>
                                <Setter Property="FontSize"        Value="11"/>
                                <Setter Property="BorderThickness" Value="0"/>
                            </Style>
                            <Style TargetType="DataGridRow">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                                <Setter Property="FontSize"   Value="13"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="{StaticResource SurfaceHover}"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="{StaticResource PrimaryLight}"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                            <Style TargetType="DataGridCell" BasedOn="{StaticResource GridCellStyle}"/>
                        </DataGrid.Resources>

                        <DataGrid.Columns>

                            <!-- CLIENT / PROSPECT avec avatar + badge statut -->
                            <DataGridTemplateColumn Header="CONTACT" Width="1.8*">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                            <!-- Avatar couleur selon statut -->
                                            <Border Width="34" Height="34" CornerRadius="17" Margin="0,0,12,0">
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Setter Property="Background" Value="{StaticResource PrimaryLight}"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding ClientStatus}" Value="prospect">
                                                                <Setter Property="Background" Value="{StaticResource WarnBg}"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>
                                                <TextBlock Text="{Binding ClientInitial}"
                                                           HorizontalAlignment="Center"
                                                           VerticalAlignment="Center"
                                                           FontWeight="Bold" FontSize="13">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Foreground" Value="{StaticResource PrimaryColor}"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding ClientStatus}" Value="prospect">
                                                                    <Setter Property="Foreground" Value="{StaticResource WarnColor}"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>
                                            </Border>
                                            <StackPanel VerticalAlignment="Center">
                                                <TextBlock Text="{Binding ClientName}"
                                                           FontWeight="Medium"
                                                           Foreground="{StaticResource TextPrimary}"/>
                                                <!-- Badge statut sous le nom -->
                                                <Border CornerRadius="4" Padding="6,1" Margin="0,3,0,0"
                                                        HorizontalAlignment="Left">
                                                    <Border.Style>
                                                        <Style TargetType="Border">
                                                            <Setter Property="Background" Value="{StaticResource PrimaryLight}"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding ClientStatus}" Value="prospect">
                                                                    <Setter Property="Background" Value="{StaticResource WarnBg}"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Border.Style>
                                                    <TextBlock Text="{Binding ClientStatus}"
                                                               FontSize="10" FontWeight="SemiBold">
                                                        <TextBlock.Style>
                                                            <Style TargetType="TextBlock">
                                                                <Setter Property="Foreground" Value="{StaticResource PrimaryColor}"/>
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding ClientStatus}" Value="prospect">
                                                                        <Setter Property="Foreground" Value="{StaticResource WarnColor}"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </TextBlock.Style>
                                                    </TextBlock>
                                                </Border>
                                            </StackPanel>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <!-- DATE badge -->
                            <DataGridTemplateColumn Header="DATE &amp; HEURE" Width="170">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border CornerRadius="5" Padding="9,4"
                                                Background="{StaticResource PrimaryLight}"
                                                HorizontalAlignment="Left"
                                                VerticalAlignment="Center">
                                            <TextBlock Text="{Binding StartAt, StringFormat='dd MMM yyyy  HH:mm'}"
                                                       Foreground="{StaticResource PrimaryColor}"
                                                       FontSize="12" FontWeight="SemiBold"/>
                                        </Border>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <!-- SUJET -->
                            <DataGridTextColumn Header="SUJET" Binding="{Binding Subject}" Width="2*">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="TextTrimming"      Value="CharacterEllipsis"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                        <Setter Property="Foreground"        Value="{StaticResource TextSecondary}"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>

                            <!-- COMMERCIAL (colonne visible pour tous, affiche le nom) -->
                            <DataGridTextColumn Header="COMMERCIAL" Binding="{Binding CommercialName}" Width="140">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="TextTrimming"      Value="CharacterEllipsis"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                        <Setter Property="Foreground"        Value="{StaticResource TextSecondary}"/>
                                        <Setter Property="FontStyle"         Value="Italic"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>

                        </DataGrid.Columns>
                    </DataGrid>

                    <!-- Message vide -->
                    <TextBlock Grid.Row="2"
                               Text="Aucun rendez-vous à afficher"
                               Foreground="{StaticResource TextMuted}"
                               FontSize="13"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               Visibility="{Binding Items.Count, ElementName=dgAppointments, Converter={StaticResource BoolToVis}, ConverterParameter=Inverted}"/>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
